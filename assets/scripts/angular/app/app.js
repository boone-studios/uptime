(function () {
  'use strict';

  angular
    .module('uptime', ['angularMoment', 'ngSanitize', 'ui.bootstrap', 'ui.router', 'templates', 'underscore'])
    .run(function ($rootScope, $state, $uiRouter, $transitions) {
      //
    });
}());
